<p id="notice"><%= notice %></p>

<p>
  <strong>Body:</strong>
  <%= @article.body %>
</p>

<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>

<%= render partial: 'comments/form', locals: { parent: @article } %>
<%= render @article.comments %>

